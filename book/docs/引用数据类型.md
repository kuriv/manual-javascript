# 引用数据类型

执行下面的代码，定义数组类型变量。

```javascript
let arr = [1, 2, 3];
console.log(arr, typeof arr);
```

执行下面的代码，定义对象类型变量。

```javascript
let obj = {};
console.log(obj, typeof obj);
```

执行下面的代码，定义函数类型变量。

```javascript
let func = () => 233;
console.log(func, typeof func);
```

